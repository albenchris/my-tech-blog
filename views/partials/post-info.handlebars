<article class="post">
    <h2 class="title">
       {{title}}
    </h2>
    <p>
        {{contents}}
    </p>
    <div class="meta">
        by {{user.username}} on {{created_at}}
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
    </div>
</article>